# Core-domain denials from the same permissive boot as the vendor file.
#
# Not everything a permissive boot logs may be allowed. Rules that AOSP
# forbids outright are left out, and the denial stays:
#
#   connfwexe system_data_root_file:dir  - init.te neverallows any domain
#     creating entries at the root of /data. The Samsung connectivity daemon
#     wants its socket there; it does not get to have one.

allow init proc:file setattr;

allow mediaserver package_native_service:service_manager find;

allow smdexe vendor_default_prop:file { getattr open read };
allow multiclientd hwservicemanager:binder call;

allow shell odsign_prop:file { getattr open read };
